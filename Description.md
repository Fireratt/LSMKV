# 项目基本功能

该项目主要实现了以LSM-Tree为基本框架的键值型数据库。

## 内存部分

内存部分使用了跳表进行实现。跳表是基于概率的KV存储数据结构，查找和插入元素的时间复杂度都为O(logN)。

跳表的最下层为一个包含所有元素的链表。在插入链表时，每个节点向上延伸的概率都为p，作为索引使用。向上的最大高度为log2n 其中n为当前元素的个数。

每升高一层的平均节点数为1/p。跳表的平均总高度为log2n，因此向下查找的高度为log2n，向右查找，找到第一个大于查找的节点只需要常数时间。

每一层都是有序的。

## Compaction

在持久化存储部分,每一层存储的SSTable个数为2^i , 当多于该个数时,需要进行合并.

vlog部分为只进行Append的数据文件.

GC进行垃圾清除时,会将过期的SStable合并删除,同时在vlog上打孔来减少硬盘占用.

### SSTable

SSTable主要记录数据为时间戳 -所有的Key - Data在Vlog上的Offset.

判断新旧:时间戳更新,且层数更靠近上层.时间戳有可能会在同样的key上重复,因为Compaction时使用的是最新的时间戳

同时还会记录key的范围以进行二分查找,加速在Scan以及Get时的速度.

### BF

LSM-DataBase有明显的缺点,在查找不存在的键时需要查找到最底层,才能确定键不存在.使用BF进行优化,记录键是否存在于DataBase中,可以减少硬盘读写.该项目给每个SSTable设置了一个BF,来避免SSTable内的无用查找.同时,内存中也有一个BF来避免不存在键的扫描.

### Cache

该项目目前采用的策略为将SSTable load到内存中进行读取,写入时在内存和硬盘中同时写入.

## 改进部分

1. 加入持久化日志，记录内存部分的操作，来避免在意外崩溃时内存部分的丢失。

2. 使用时钟算法实现类似LRU的机制来管理缓存.

3. 事务管理.

4. SSTable Cache可以仅写内存来减少硬盘反复读写,同时可以一定程度上避免触发Compaction.